## Trace API JavaScript sample app

This repo contains an example JavaScript app for interacting with the Trace API. The program prints the number of test cases that passed a given submission.

To run:

```
yarn
node index.js
```